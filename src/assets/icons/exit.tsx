import { IconElement } from "@ui-kitten/components";

import { Icon } from "@ui-kitten/components";
import { ImageStyle } from "react-native";

export const ExitIcon = (style: ImageStyle): IconElement => (
  <Icon {...style} name='log-out-outline'/>
);